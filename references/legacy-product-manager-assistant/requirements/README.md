# 需求（`requirements/`）

这里保存所有 Requirement Workspace。

规则：
- 一个需求一个独立文件夹。
- 推荐命名：`REQ-编号-需求名称`；没有编号可直接用清晰需求名。
- 不按 Skill 名称建立用户目录。
- 原始资料与产品输出分开保存。
- 成果使用 `source / analysis / solution / flows / prototype / prd / review / changes` 英文稳定路径；中文名称仅用于界面和文档标题。
- 创建新需求时复制 `_template/`；不要从 `.agents/` 复制模板。
